# Base web and test
PyGithub==2.2.0
requests==2.32.0
python-dotenv==1.0.1
aiohttp==3.10.0
Flask==3.0.0
flask-cors==4.0.0
fastapi==0.109.0
uvicorn[standard]==0.27.0
websockets==11.0.3
python-socketio==5.11.0
httpx==0.28.0
pytest==7.4.0
pytest-cov==4.1.0
pytest-anyio
trio

# Token counting
tiktoken==0.7.0

# RAG / Vector store (optional, with graceful fallback in code)
chromadb==0.4.22
sentence-transformers==2.2.2

# NOTE:
# - LangChain provider packages are intentionally excluded to avoid
#   dependency conflicts on Python 3.8. The app includes fallbacks.
# - To enable specific providers, install extras manually:
#     Groq:    pip install langchain-groq>=0.1.6
#     Ollama:  pip install langchain-ollama>=0.1.3
#     Chroma:  pip install langchain-chroma>=0.1.4
